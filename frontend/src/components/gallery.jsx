import React, { Component } from 'react';
import PropTypes from 'prop-types';

/**
 * Cart
 */
export class Gallery extends Component { // eslint-disable-line react/prefer-stateless-function
  render() {
    return (
      <div>
        <h1>
          Gallery
        </h1>
      </div>
    );
  }
}

// Gallery.propTypes = {
//   prop: PropTypes.type.isRequired
// }

export default Gallery;
